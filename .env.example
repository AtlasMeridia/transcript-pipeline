# =============================================================================
# Transcript Pipeline Configuration
# =============================================================================
# Copy this file to .env and fill in your values

# =============================================================================
# Transcription Engine
# =============================================================================
# Choose transcription backend: 'whisper' (local) or 'elevenlabs' (cloud)
# Default: whisper
TRANSCRIPTION_ENGINE=whisper

# -----------------------------------------------------------------------------
# Whisper Configuration (local transcription)
# -----------------------------------------------------------------------------
# Model options: tiny, base, small, medium, large, large-v2, large-v3
# Larger models are more accurate but slower and use more memory
# Default: large-v3
WHISPER_MODEL=large-v3

# Directory to cache downloaded Whisper models
# Default: ~/.cache/whisper
# WHISPER_MODEL_DIR=/path/to/models

# -----------------------------------------------------------------------------
# ElevenLabs Configuration (cloud transcription)
# -----------------------------------------------------------------------------
# Required if TRANSCRIPTION_ENGINE=elevenlabs
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# ElevenLabs Scribe model ID
# Default: scribe_v2
# SCRIBE_MODEL_ID=scribe_v2

# =============================================================================
# LLM Configuration (for transcript extraction/summarization)
# =============================================================================
# Choose default LLM: 'claude' or 'openai'
DEFAULT_LLM=claude

# Anthropic (Claude) - required if DEFAULT_LLM=claude
ANTHROPIC_API_KEY=your-anthropic-api-key-here
# CLAUDE_MODEL_ID=claude-sonnet-4-5

# OpenAI - required if DEFAULT_LLM=openai
# OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_MODEL_ID=gpt-4o-mini

# =============================================================================
# Output Configuration
# =============================================================================
# Directory for transcripts and summaries
# Default: ./output
# OUTPUT_DIR=./output

# =============================================================================
# Server Configuration
# =============================================================================
# CORS origins (comma-separated list, use * for development)
# CORS_ORIGINS=*

# Port for the server (Railway sets this automatically)
# PORT=8000
